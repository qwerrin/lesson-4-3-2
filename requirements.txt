google-api-python-client==2.198.0
google-auth==2.56.3
google-auth-httplib2==0.4.1
google-auth-oauthlib==1.4.0
# 課題4（Zoom）で直接使う。google-auth-oauthlib の依存として元から入っていたが、
# 直接 import する以上、依存が外れたときに気づけるよう明示する。
requests==2.34.2
pytest==9.1.1
# 課題9（LINE）だけ資格情報を .env で渡すため。課題4〜8 は PowerShell の
# 環境変数だったので、ここまで依存が無かった。
python-dotenv==1.2.3
